# 建築見積「項目出し」高精度化 計画書（工程チェーン展開版）

> 位置づけ：開発着手前のブレスト記録。
> 方針転換：背骨を「過去見積」や「科目の網羅」ではなく、**表に出る項目から前工程を標準展開する工程チェーン辞書**に置く。

---

## 0. 中核思想

### 原則1：表項目から前工程チェーンを全展開する
見積の単位を「表に出る仕上（クロス貼り等）」とし、そこから**実際に発生する前工程の連鎖**を標準分解して出す。
例）クロス：`下地ボード → ジョイント処理（寒冷紗/ファイバーテープ）→ パテ（下塗り・上塗り）→ シーラー → クロス貼り → ボンドコーク → 養生 → 残材処分`
表に出ない前工程（パテ・シーラー・養生・処分…）が抜けると、作業は発生するのに請求されず原価が利益を食う。**完全分解＝収益率の保護。**

### 原則2：recall最優先、過剰計上は許容
- **漏れ＝ツールの存在意義の否定**（自分の知識で補うなら、ツールが無い時と同じ）。
- **過剰＝許容**。現場事情に合わない項目は人間がチェックを外すだけで数分で消せる。
- よって判定は迷ったら**出す**。落とすのは「積極的に非該当の根拠があるとき」だけ。

### 原則3：ゼロから発明しない（出典で精度と正当性を担保）
工程チェーンは勘で作らず、一次資料に紐づける。

---

## 1. スコープと成功の定義

### スコープ
- 建物種別を1つに固定して開始（精度に最も効く）。
- 出力は**項目（工程）リストまで**。数量・単価は人間が埋める。

### 成功の定義（メトリクス）
- **Recall ＝ ほぼ唯一の主指標**。「その仕上で実際に発生する作業が、前工程まで含めて出ているか」。
- Precisionは緩い制約のみ：「不要項目の削除が人手で数分に収まる量か」。過剰そのものは減点しない。
- ただし**確度ラベル別に測る**（Phase 3）。特に「標準」ラベルのprecisionは高く保つ（芯が汚れると全体の信用が落ちる）。低確度の過剰は許容。
- 目標例：「正解チェーンに対し recall ◯◯%以上／標準ラベルのprecision ◯◯%以上／過剰は削除◯分以内」。数値はPhase 0で確定。

---

## 2. フェーズ計画

### Phase 0 — 評価基盤（最優先・ここから着手）
**過去見積は生成のソースにしない。** ただし扱い方に罠がある。

- 罠：過去見積を**正解データ**にすると、過去見積自体が前工程を抜いているため、ツールが正しく前工程を出したのを「誤検出」と減点してしまう。
- 対策：
  - **正解データ＝父＋自分で作る「完全分解チェーン」**（その仕上に本来必要な全工程を列挙したもの）。
  - **過去見積は「漏れの実例集」**として参照する（＝どの前工程が現場で抜けがちかの教師）。
- recall / precision 計算スクリプトを用意。
- spec.md流「テスト＝done」を適用：完全分解チェーン集が done の定義。

### Phase 1 — 背骨：工程チェーン辞書の構築

**正解の出典＝公共建築工事標準仕様書 ＋ JASS（日本建築学会 建築工事標準仕様書）。**
辞書の各工程は、必ずこのどちらかに素性を持つ。

- **トリガー＝表に出る項目／仕上**（クロス、塗装、フローリング、タイル…）。
- 各トリガーに**標準工程チェーン**（材料・作業を含む連鎖）を定義。
- 各工程に**確度メタデータ**を持たせる（後述の出自で機械的に決まる。Phase 3）。

#### 辞書の関心事は3レイヤーに分離する
1. **工種固有の工程チェーン**（trigger → 前工程連鎖）＝本辞書の本体
2. **各工程の確度メタデータ**（出自から機械決定）
3. **案件横断の付帯テンプレート**（後述、辞書とは別レイヤー）

#### 出典の二段構え（役割分担）
- **公共建築工事標準仕様書 ＝ 工種の地図**：建築工事全体を横断。工種の網羅と標準的な施工順序の骨格づくりに使う。
- **JASS ＝ 各工種の深掘り**：編ごとに特定工事を詳述（JASS 8防水 / JASS 15左官 / JASS 18塗装 / JASS 26内装 …）。**個別工種の前工程チェーンを細かく埋めるのはJASS側**。
- 進め方：標準仕様書で工種地図を作り、JASSで各工種の中を掘って前工程を埋める。

#### 辞書化の実務上の注意
- **仕様書は「品質基準」であって「工程の連番リスト」ではない**。材料規格・下地状態・養生日数などの規定が主で、作業の番号付きリストでは書かれていない。よって機械的な抽出ではなく、規定文から**作業ステップを再構成する読み取り作業**になる。ここは父の実務知で組み、仕様書は裏取り・抜け確認に使う、という関係。
- **条文そのものは辞書に転記しない**（権利面のリスク回避）。格納するのは「工程名 ＋ 出典参照」にとどめる。
- 各工程に**出典タグ**を持たせる：`source: "JASS 18" / "標準仕様書 ◯章"`。仕様書は改訂されるため、改訂時に「どの工程を見直すか」を追える版管理用の資産になる。

- **分岐は「良いデフォルトのため」に持つが、抑制には使わない**：新設/改修・下地種別・グレードで標準形を切り替える。ただし迷う場合は全工程を出し、確度タグを下げる。

#### 付帯項目は工程チェーンに焼き込まない（別レイヤー）
養生・運搬・残材処分・産廃・諸経費・仮設は「工種固有の前工程」ではなく「案件全体にかかる費目」。まとめ方（共通仮設に寄せる／各工種へ按分／一式計上）が会社・案件で変わるため、チェーンに混ぜると粒度が破綻する。

- これらは**共通付帯テンプレート**として独立レイヤーに切り出す。
- 工程チェーンには含めず、展開後の最後に**別ロジックで合流**させる（合流方式＝按分/一式/共通仮設をユーザーが選べる余地を残す）。

### Phase 2 — 入力の構造化抽出（変更なし）
- 仕上表 → 室別仕上仕様、建具表 → 建具リスト、図面注記・凡例 → テキスト。
- 出力：事実JSON（種別/構造/階数/延床/室別仕上/建具/外部仕上…）。
- ラスタ図面からの幾何実測は対象外。

### Phase 3 — 判定（方向を反転：原則展開・例外だけ削る）
1. **チェーン全展開**：事実JSONの各トリガーに対し、辞書のチェーンを原則すべて展開。
2. **LLMの役割は「落とす根拠探し」**：各工程について「非該当の積極的根拠があるか」だけを判定。根拠が無ければ**残す**。
3. **根拠グラウンディング**：各工程に「どの資料・どの条件から来たか」を併記（検証高速化）。
4. **確度ラベル（4段階・出自で機械的に決まる）**：
   recall最優先を貫きつつ、ユーザーが視線を配分できるよう「出すか落とすか」ではなく「どの確度で出すか」で階層化する。過剰の本当のコストは項目数より「全項目を等価に検証させられること」なので、ここを確度で解く。
   ラベルは人間の主観で付けず、**辞書のメタデータと事実JSONの突合結果＝出自**から自動決定する（tests=doneで検証可能・AI展開時も再現可能）：
   - **標準**：出典タグ（標準仕様書/JASS）があり、条件分岐なしで常時発生 → 信じて流せる芯
   - **条件付き**：条件分岐にヒットし、事実JSONで条件が確定して出た
   - **要確認**：該当する条件が事実JSONからは確定できず、推定で出した
   - **低確度（かなり怪しい）**：トリガーや条件の根拠が弱く、漏れ防止のため念のため出した
   UIは確度で段階表示し、「標準は畳んで流す／低確度だけ目視」を可能にする。

### Phase 4 — 欠落検出（recall強化＝核心機能）
- **「表項目はあるのに前工程が欠けている」状態を検出して警告**。これが過去見積依存では出てこない、本ツール固有の価値。
- 複数プロンプト／複数モデルで候補をunion（Claude/ChatGPT/Gemini並走を漏れ検出に活用）。
- 削除UIは極力軽く（チェックを外すだけ）。過剰許容方針なので、追加より削除が主操作。

### Phase 5 — 出力と検証、フィードバック還流
- 工程ごとに根拠・三層タグを併記。数量列は空欄で人手入力前提。
- 人間の ✓/✗ を Phase 0 の評価データ（完全分解チェーン）に還流。
- 出力：工種別に整理した項目リスト（Excel/CSV）。

---

## 3. 精度を上げるレバー（効く順）

1. **工程チェーン辞書の網羅性**（最大レバー）— 前工程を取りこぼさない辞書がすべて
2. **recall最優先 ＋ 確度の階層化**（標準/条件付き/要確認/低確度）— 全部フラットに出さず、出自で確度を機械決定して信用の芯を守る
3. **出典に基づくチェーン構築**（標準仕様書で工種地図、JASSで前工程深掘り、各工程に出典タグ）
4. **付帯は別レイヤー**（養生・処分・諸経費・仮設をチェーンに焼き込まず最後に合流）
5. **「表項目あり／前工程なし」の欠落検出**
6. **建物種別スコープを絞る**

---

## 4. 非対象・既知のリスク

- 非対象：ラスタ図面の幾何実測、汎用単価DB（持たない）。
- リスク：過剰の許容にも上限がある。「明らかに非常識な量」になると信頼を失い、削除が数分で済まなくなる → 条件タグで現実的なデフォルトに寄せる。
- リスク：チェーン辞書の品質＝ツールの品質。最初の辞書づくり（知識エンジニアリング）が事実上のコア開発。仕様書は連番の工程表ではないため、規定文から作業ステップを再構成する読み取りが必要で、ここに父の実務知を要する。
- リスク：仕様書（標準仕様書・JASS）は改訂される。出典タグで影響範囲を追える設計にしておく。条文の転記・再配布は避け、辞書には工程名＋出典参照のみ格納する。
- リスク：総合図1枚では情報不足。仕上表・建具表が揃うほど精度が上がる。

---

## 5. 最小の最初の一歩

> Phase 0 と Phase 1 を、対象を1仕上だけに絞って同時に着手する。
> 例：「クロス貼り」一本について、父と一緒に**完全分解チェーン**（前工程含む正解）を作る。
> 同時に手元の過去見積でそのクロス工事を見て、**どの前工程が抜けていたか**を1つでも見つける。
> この1サンプルが「チェーン辞書の作り方」と「漏れ検出の効きめ」を同時に検証し、以降の設計を規定する。
