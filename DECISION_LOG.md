# DECISION_LOG.md

本ドキュメントは、「見積項目出し_高精度化（construct_quotation）」プロジェクトの設計・技術選定・スコープ決定に関する意思決定の背景と帰結を記録するものです。

---

## 1. 意思決定一覧

### [2025-12-21] 1. 評価指標（Recall最優先・過剰計上の許容）の決定

- **ステータス**: 承認
- **背景**: 建築見積における最大の損失は「施工が発生するにもかかわらず、項目そのものを見積書に載せ忘れる（請求漏れ）」ことである。これを防ぐには、網羅性を最大化しなければならない。
- **決定事項**: 
  - 本システムの主要評価指標は **Recall（再現率）** とし、目標値を **98%以上** に設定する。
  - 漏れを防ぐための「過剰な計上（不要な工程の出力）」は許容する。削減工数を無駄にしないため、不要項目はUI上で人間が数分でチェックを外して一括削除できる仕組みとする。
- **帰結**: Precision（適合率）の低下による多少のノイズはUI側（折りたたみ・確度ラベル）でフィルタリング可能とし、開発難易度の極端な上昇を避ける。

---

### [2025-12-21] 2. 数量拾い出し・図面情報の完全非対象化

- **ステータス**: 承認
- **背景**: 先行研究調査（BIM連携や画像解析AI）では、図面からの数量算出やBIMモデリング基準の設計に膨大な工数がかかり、導入障壁となっていることが判明した。本プロジェクトでは速やかな検証と実務投入（MVP）を目指す。
- **決定事項**:
  - CAD、PDF図面、BIM(3Dモデル)データなどのグラフィカルな情報のパース、およびそれらに基づく幾何学的な「数量算出（面積・長さ等の計算）」は一切対象外とする。
  - 本ツールは、テキストベース（自由記述プロンプト、仕上げ表・室仕上表・建具表のExcelデータ）から「必要な項目（工程）」の抜け漏れを自動でチェック・展開する機能に100%特化する。
- **帰結**: プロジェクトのスコープがシンプルになり、テキスト処理（LLM）と工程辞書（マスタデータ）の整合性ロジックに開発を集中できる。

---

### [2025-12-21] 3. 標準仕様書・JASSの紐付けと条文転記の禁止

- **ステータス**: 承認
- **背景**: 工程チェーンの妥当性を担保するために公共建築工事標準仕様書やJASS（日本建築学会）に準拠する必要があるが、条文テキストそのものをデータベースに格納・表示することは、権利上のリスクおよび辞書メンテナンス（仕様書改訂時の追従負荷）の面で好ましくない。
- **決定事項**:
  - 各工程には「出典タグ（例: `source: "JASS 26"`）」のみを付与し、条文そのものの転記や再配布は行わない。
  - 仕様書は、工程ステップの再構成および人間のチェック時の「裏取り」用として参照し、システム内部には項目名と参照コードのみを保持する。
- **帰結**: 法的リスクを完全に排除しつつ、仕様書改訂時に「どの工程定義を見直すべきか」の影響範囲をコードから即座に追跡できる資産価値を持たせる。

---

### [2025-12-21] 4. 技術スタック（Next.js + Node.js）の選定

- **ステータス**: 承認
- **背景**: ユーザーがExcelファイルをアップロードして項目を確認し、不要なものを1クリックで削除・マークダウン出力する対話型UI（例外キュー）を構築するには、フロントエンド主体のWebアプリが最適である。
- **決定事項**:
  - フレームワークに **Next.js (TypeScript)** を採用する。
  - 見積という極めて機密性の高い情報を取り扱うため、基本的にはサーバー側にファイルを永続化せず、ブラウザ側またはNext.jsのAPIルートを中継するのみのセキュアな構成をとる。
  - スタイリングは標準環境のVanilla CSS / CSS Modulesをベースとする。
- **帰結**: サーバーレスで運用コストが極めて低く、データの機密性を担保したWebアプリケーションが構築可能になる。

---

### [2025-12-21] 5. 実務知と仕様書参照のデータ構造（統合と権利保護）の決定

- **ステータス**: 承認
- **背景**: 「見積項目出し」のコアとなる工程チェーン辞書は、公的な仕様書と実務者の現場ノウハウの双方を反映したものである必要がある。また、著作権保護の観点から仕様書の本文を直接DB等に含めることは回避しなければならない。
- **決定事項**:
  - 各工程（`ProcessStep`）は、複数の根拠を持てるように `sources: ReferenceSource[]` スキーマを導入する。
  - 出典タイプを公的仕様書（`standard_spec`, `jass`）と実務知（`practical_knowledge`）に区分し、標準的な命名コード規則を決定する。
  - UIでは本文を表示せず、出典コードのバッジのみを表示し、手元の一次資料の参照を促す「Human-in-the-loop」設計に準拠する。
- **帰結**: 仕様書の利用ライセンスや著作権侵害のリスクを完全に回避しつつ、実務に適合したかつ検証可能な高精度なマスタデータの構築が可能になる。

---

### [2026-06-07] 6. 環境分離(Junction)とNext.js 16 (Turbopack) のビルド競合解決

- **ステータス**: 承認
- **背景**: Next.js 16 から Turbopack がデフォルトビルドエンジンとなった。Turbopack はセキュリティおよびキャッシュ整合性向上のため、プロジェクトルートより外側への Junction/シンボリックリンク解決（今回の共有 node_modules）を「境界外エラー」として拒否する仕様である。
- **決定事項**:
  - Webpack へのフォールバックはビルドパフォーマンス上好ましくないため、`next.config.ts` で `turbopack.root` および `outputFileTracingRoot` を `../../../` (親の `D:\dev` 階層) に設定して Turbopack の探索境界を安全に拡張する。
  - 不要な `node_modules` バックアップが TypeScript にスキャンされ型エラーになるのを防ぐため、セットアップスクリプトをリネームではなく直接削除するように修正する。
- **帰結**: 環境分離（Junction）の仕組みを損なうことなく、Next.js 16 + Turbopack の高速ビルド性能を享受しつつ、安定した開発ビルド環境を確立できる。

---

### [2026-06-07] 7. テスト自動化環境（Jest）の導入と単体テストの実装

- **ステータス**: 承認
- **背景**: パースや工程展開ロジックの信頼性を将来にわたって維持するため、また実務データを用いた評価をスムーズに行うため、自動テスト環境が必要不可欠である。
- **決定事項**:
  - `next/jest` ユーティリティを利用して、Next.js 16 (SWC) のトランスパイルを利用した Jest 自動テストを導入する。
  - Excel parser (`excelParser.ts`) と工程マッチャー (`matcher.ts`) に対する網羅的な単体テストを `src/utils/__tests__` 配下に実装する。
  - テストデータとして実データに近いダミーExcelファイル (`test_finishing_schedule.xlsx`) を利用し、実際のパースからデータ展開までの結合挙動もテスト内でカバーする。
- **帰結**: 今後の辞書データの拡張やパース処理の複雑化の際にも、リグレッションを自動で検知でき、堅牢なメンテナビリティを確保できた。

---

### [2026-06-07] 8. LLM連携と共起警告、およびフィードバックログによるデータ還流環境の実装

- **ステータス**: 承認
- **背景**: 自由記述プロンプトからのパースや不要工程の自動除外理由の表示、および除外の妥当性を高めるための還流環境は、MVP段階から実効検証を行う必要がある。
- **決定事項**:
  - OpenAI API (gpt-4o-mini) の Structured Outputs を用いて、テキストから仕上げ情報の抽出 (`api/parse-text`)、およびコンテキストに基づく不要工程の自動特定 (`api/filter-processes`) を実装。
  - 工種間の建築論理チェックを行う共起エンジン (`cooccurrenceEngine.ts`) を導入し、重要な前工程の不足を「計上漏れ警告」として UI 上に赤色バッジで可視化。
  - ユーザーがエクスポートを行った際に、採用・除外の選択履歴を JSONL 形式のフィードバックログ (`feedback_logs.jsonl`) に蓄積する API を構築。
  - 蓄積されたログから工程ごとの除外率や理由を集計し、マスタ辞書のメンテ箇所を洗い出すデータ還流バッチ (`scripts/feedback-loop.js`) を作成。
- **帰結**: ロードマップの Phase 2〜5 で要求された全コア機能を網羅的に実現し、実務で実際に使える高精度な「項目出し」MVP環境が整った。

---

### [2026-06-07] 9. 「項目出し」特化のオンライン完結型自動収集・パースと整形検証方針

- **ステータス**: 承認
- **背景**: 将来のナレッジ収集における人手依存（インタビューや伝達シート）を排除し、オンライン上のみで非対話完結させるため、レイアウト解析やドキュメント分割・ナレッジグラフ化などの自動処理技術に特化した調査を行い、方針を再定義した。
- **決定事項**:
  - **オンライン自動収集手法の高度化**:
    1. **Donut/VGTによるOCRフリー階層抽出**: 画像から目次階層（ToC）やリスト・表・Key-Value領域の2Dレイアウトを直接JSON化する。
    2. **Vision-Guided Chunkingによる文脈維持**: PDF画像を LMM で処理し、3レベル階層の強制と `[CONTINUES]` フラグによる関連ページの自動マージを行い、施工ステップの断絶を防ぐ。
    3. **LLM・ナレッジグラフによる自動要件パース**: Transformerベースモデルを用いてテキストからコンセプト（部材）とプロパティ（品質仕様）を抽出し、相関関係をナレッジグラフ化する。
  - **整形・適用手法の高度化**:
    1. **表記揺れ自動正規化と同義語マッピング**: 既存見積書の階層（大中分類）を維持して読み込み、自社標準工種へ自動分類・名寄せする（ミャク楽・工事ロイドの実例に準拠）。
    2. **CPM（クリティカルパス）工程順序制約エンジン**: 各工程データに順序連番（`sequenceIndex`）を組み込み、先行工程（before/after）や接続・ラップ条件を順序制約エンジンで論理検証し、見積画面で警告を出す。
  - **RAGの拡張方針**:
    1. **2層ベクトルRAG (Local/Global RAG)**: WBS定義等の専門用語をLocal RAG、標準仕様書の施工解説長文をGlobal RAGとしてベクトルDB化し、LLMフィルタの除外判定コンテキストとして与える。
- **帰結**: 人手を一切介さないオンライン自動パース・構造化パイプラインおよびCPM順序制約チェックによる堅牢な「項目出し」高度化ロードマップを決定した。


---

### [2026-06-10] 10. フィードバック還流と辞書永続化の Cloudflare D1 移行

- **ステータス**: 承認
- **背景**: Worker の `/feedback` は受信ログを stdout に出力するだけで永続化されず、管理画面のナレッジ抽出結果も localStorage 保存のみだったため、本番環境（静的Pages + Worker）では「ユーザー判定の正解データへの還流」と「マスタ辞書の1クリック永続化」という Phase 5 / Phase 6 の核心機能が成立していなかった。
- **決定事項**:
  - 永続化ストアとして **Cloudflare D1（SQLite）** を採用する（`api-worker/schema.sql`）。集計クエリが必要なフィードバックログにはSQLが適しており、辞書のupsertも同一DBに統合してインフラを単純化する。
  - `/feedback` はフロントから送信される工程ごとの採用・除外詳細（`details`）を `feedback_events` / `feedback_details` に保存する。旧クライアント形式（processList + excludedIndices）からの導出にも対応し、**D1 バインディング未設定時は従来の stdout ログへフォールバック**して後方互換を維持する。
  - 管理者キー保護の `GET /feedback/summary` で工程ごとの提案数・除外数・除外率・除外理由を集計返却し、`scripts/feedback-loop.js` は環境変数（`FEEDBACK_API_BASE` + `ADMIN_KEY`）指定でこのリモート集計を利用できるようにする（ローカル JSONL 集計も併存）。
  - 管理者キー保護の `POST /update-dictionary` を新設し、スキーマ検証（`validateDictionary.ts`）を通過した辞書JSONのみ D1 の `dictionaries` テーブルへ upsert する。`/extract-knowledge` も抽出成功時に自動で D1 へ永続化し、`/get-dictionaries` は静的辞書に D1 のオーバーライドをマージして返す。
- **帰結**: エクスポート時のユーザー判定が本番環境で恒久的に蓄積され、除外率の高い工程（辞書の偽陽性候補）をオンラインで特定できるデータ還流サイクルが成立した。また管理画面のナレッジ抽出が全ユーザー共有のマスタ辞書として永続化され、ロードマップの「1クリック永続化」要件を満たした。
